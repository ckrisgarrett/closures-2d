#PBS -A PHY011
#PBS -N HAUCK_ENTROPY
#PBS -j oe
#PBS -V
#PBS -l walltime=5:00,nodes=4

cd $PBS_O_WORKDIR
rm /tmp/work/$USER/*
cp *.deck /tmp/work/$USER
cp *.x /tmp/work/$USER
cd /tmp/work/$USER

export OMP_NUM_THREADS=16
aprun -n4 -d16 ./solver_mpiomp.x > output.txt

